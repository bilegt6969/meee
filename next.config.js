/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['khas-dayan.api.erxes.io', 'via.placeholder.com'],
  },
  // ... other existing config
};

module.exports = nextConfig;
